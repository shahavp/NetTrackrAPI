# CPU-only PyTorch (keeps image ~2 GB instead of ~8 GB)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision

# FastAPI + server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# ML + CV
ultralytics>=8.0.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
pandas>=2.0.0
lap

# Validation
pydantic>=2.0.0