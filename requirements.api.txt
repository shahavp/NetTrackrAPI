# API service dependencies only — no torch, no ultralytics, no OpenCV.
# This keeps the API image small (~150 MB) and cold start fast.

# FastAPI + server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Validation
pydantic>=2.0.0

# Redis job state + RQ queue
redis>=5.0.0
rq>=1.16.0

# S3-compatible bucket client (Railway Bucket)
boto3>=1.34.0
